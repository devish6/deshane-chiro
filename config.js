/* ============================================================
   De Shane Chiropractic — site configuration
   Edit the two values below, then commit & push.
   ============================================================ */
window.DESHANE_CONFIG = {

  // 1) Your SheetDB API URL. Create a Google Sheet (see README), connect it
  //    at https://sheetdb.io, and paste the endpoint here. Until you do, the
  //    site shows the built-in default address/hours and no alert bar.
  SHEETDB_API: "https://sheetdb.io/api/v1/qeplzuvyue6mp",

  // 2) Admin password for the /admin.html control panel.
  //    NOTE: this is a *soft* gate — on a static site the password lives in
  //    this file (visible in the public repo). Pick something non-obvious.
  ADMIN_PASSWORD: "ReDa2026"
};
